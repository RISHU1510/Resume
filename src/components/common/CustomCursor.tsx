import React from 'react';

/**
 * CustomCursor component.
 * Native hardware-accelerated SVG cursors in classic black & red
 * are handled directly in CSS (src/index.css) for zero latency,
 * instant responsiveness, and standard cursor ergonomics.
 */
export const CustomCursor: React.FC = () => {
  return null;
};
